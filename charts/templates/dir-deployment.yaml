apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.siteminder.dir.name }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.siteminder.dir.name }}
  replicas: 1 
  revisionHistoryLimit: 0
  progressDeadlineSeconds: 30
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: {{ .Values.siteminder.dir.name }}
    spec:
      containers:
        - args:
            - /sbin/init
          image: pconnor/cadir:latest
          name: {{ .Values.siteminder.dir.name }}
          ports:
            - containerPort: 19389
            - containerPort: 15389
            - containerPort: 16389
            - containerPort: 9443
          resources: {}
          securityContext:
            privileged: true
          volumeMounts:
            - mountPath: /sys/fs/cgroup
              name: {{ .Values.siteminder.dir.persistence.name }}
              readOnly: false
      hostname: {{ .Values.siteminder.dir.host }}
      restartPolicy: Always
      volumes:
        - name: {{ .Values.siteminder.dir.persistence.name }}
          persistentVolumeClaim:
            claimName: {{ .Values.siteminder.dir.persistence.name }}
            readOnly: false
status: {}

